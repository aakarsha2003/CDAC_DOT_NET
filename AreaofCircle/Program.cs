using System;
class AreaofCircle
{ 
    static void Main()
    {
        double radius = 3.14;
        double area = Math.PI * radius * radius;
        Console.WriteLine("Area of circle:" + area);
    }
}
